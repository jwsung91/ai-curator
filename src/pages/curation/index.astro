---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const allCuration = await getCollection('curation');
const sortedCuration = allCuration.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<BaseLayout title="Daily Reports Archive — AI Curator" description="AI가 수집하고 요약한 데일리 테크 리포트 아카이브">
  <section class="mb-12 border-b border-stone-100 pb-8 dark:border-stone-900">
    <h1 class="text-4xl font-bold text-stone-900 dark:text-stone-100 md:text-5xl">Daily Reports</h1>
  </section>

  <section>
    <div class="mb-8 flex items-baseline justify-between border-b border-stone-100 dark:border-stone-900 pb-4">
      <h2 class="font-mono text-xs uppercase tracking-wider text-stone-500">Daily Archive</h2>
      <span class="font-mono text-[10px] text-stone-400">{sortedCuration.length} reports</span>
    </div>

    {sortedCuration.length === 0 ? (
      <div class="rounded-lg border border-dashed border-stone-300 dark:border-stone-700 p-8 text-center">
        <p class="mb-1 text-sm font-medium text-stone-700 dark:text-stone-300">아직 생성된 리포트가 없습니다</p>
        <p class="text-xs text-stone-500 dark:text-stone-500">전날의 로보틱스/AI 기술 소식이 KST 06:00에 자동 발행됩니다</p>
      </div>
    ) : (
      <ul class="divide-y divide-stone-100 dark:divide-stone-900">
        {sortedCuration.map((report) => (
          <li>
            <a
              href={`${base}/curation/${report.id}/`}
              class="block py-6 transition-colors duration-200 hover:text-azure-600 dark:hover:text-azure-400"
            >
              <p class="mb-2 text-xl font-semibold text-stone-900 dark:text-stone-100">
                {report.data.title}
              </p>
              <p class="text-base leading-relaxed text-stone-500 dark:text-stone-500 line-clamp-2">
                {report.data.summary}
              </p>
            </a>
          </li>
        ))}
      </ul>
    )}
  </section>
</BaseLayout>
